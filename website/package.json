{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run tokens && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "tokens": "esbuild scripts/generate-tokens.mjs --bundle --platform=node --format=esm --outfile=node_modules/.cache/generate-tokens.mjs --log-level=warning && node node_modules/.cache/generate-tokens.mjs",
    "astro": "astro",
    "nodes": "node scripts/generate-node-pages.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.9",
    "@fontsource-variable/archivo": "^5.3.0",
    "astro": "^7.2.8",
    "astro-icon": "^1.2.0",
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@iconify-json/material-symbols": "^1.2.90",
    "@material/material-color-utilities": "^0.4.0",
    "esbuild": "^0.28.2",
    "typescript": "^6.0.3"
  }
}
