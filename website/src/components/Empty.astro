---
/**
 * Renders nothing.
 *
 * Stands in for Starlight's ThemeSelect: the site commits to one look, so there is
 * nothing to select. Note that this alone does not disable light mode — see the
 * localStorage note in `starlight-theme.css`.
 */
---
