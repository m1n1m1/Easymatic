config:
  validation: true
  warningsAsErrors: false

naming:
  FunctionNaming:
    active: true
    ignoreAnnotated: ['Composable', 'Preview']

complexity:
  active: true

potential-bugs:
  active: true
