# The locale that `values/` (no qualifier) actually holds.
#
# Required by `androidResources.generateLocaleConfig`: AGP builds
# res/xml/locales_config.xml from the values-* folders present, and without this it
# cannot know what the unqualified folder is written in.
unqualifiedResLocale=en-GB
