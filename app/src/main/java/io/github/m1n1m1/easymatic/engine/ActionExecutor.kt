package io.github.m1n1m1.easymatic.engine

// The old recursive ActionExecutor has been replaced by [WorkflowExecutor],
// which walks the two-channel (execution + data) graph and runs the new
// typed Action contract. See WorkflowExecutor.kt.
