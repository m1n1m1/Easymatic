package com.example.ottomatic.engine

// The `Action` interface and `interpolate` helper previously declared in this
// file have moved:
//  - Action       -> Payload.kt   (alongside ActionInput and ActionResult)
//  - interpolate  -> TypedConfig.kt (alongside the TypedConfig.expr accessor)
